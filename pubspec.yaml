name: fluid_grid
description: A lightweight and responsive data grid for Flutter with adaptive layouts, custom cell rendering, and support for web, desktop, and mobile.
version: 0.0.3

repository: https://github.com/MayurMadhwani/fluid_grid


environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

topics:
  - datagrid
  - data-table
  - responsive
  - flutter-web-android-ios-desktop
  - lightweight